<div class="extras">

    <div id="stats" class="box">
		<h3>Prime Numbers</h3>
		<p>Prime Number Solution: <%= @sum %></p>
		<p>Sum of all primes up to 10: <%= @sum = Primes.sum_to(10) %></p>
		<p>Sum of all primes up to 10000: <%= @sum = Primes.sum_to(10000) %></p>
		<p>Sum of all primes up to 2000000: <%= @sum = Primes.sum_to(2000000) %></p>
  		<p><a href="/">Back</a></p>
	</div>

</div>